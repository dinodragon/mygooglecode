/**************************************************
    进行一些简单的链表相关操作练习

                 程序设计：杨飞
				 http://yangfei.vicp.cc
***************************************************/

#include<iostream>
using namespace std;

void main()
{
	struct node
	{int data;
	 struct node * next;
	}
	typedef struct node Node;

	int a,b;
	Node *p, *q, *s, *head;
	for(int i = 1 ; i < 10 ; i++)
	{
		s = (Node *)malloc(sizeof(Node));
		
	}
		
}